services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: memeapp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - memeapp_pg:/var/lib/postgresql/data

volumes:
  memeapp_pg:
